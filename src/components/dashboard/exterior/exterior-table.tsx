'use client';

import * as React from 'react';
import Typography from '@mui/material/Typography';

export function ExteriorTable(): React.JSX.Element {
  return (
    <div>
      <Typography variant="h4">Página Exterior Funcionando!</Typography>
    </div>
  );
}
